import { useLoaderData } from "react-router-dom";


const Phone = () => {
    const phone = useLoaderData()
    
    return (
        <div>
            
        </div>
    );
};

export default Phone;